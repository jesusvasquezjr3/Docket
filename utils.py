# Placeholder for utility functions (e.g., gradient background)
def create_gradient(canvas, width, height, color1, color2):
    # Simplified gradient simulation (could be expanded with a real gradient)
    canvas.create_rectangle(0, 0, width, height, fill=color1, outline="")
